<% if @batch.present? %>
	<header>
		<%= link_to "Back", new_bean_path(:batch_id => @batch.id, :roast_id => @roast.id)  %>
	</header>
<% else %>
	<header>
		<%= link_to "Back", farms_path %>
	</header>
<% end %>
<%= render 'form' %>

