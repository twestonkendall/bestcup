<header>
	<%= link_to 'Back', farm_path(@bean.farm) %>
</header>
<%= render 'form' %>
<% if can? :destroy, @bean %>
	<%= button_to 'Destroy', @bean, :confirm => 'Are you sure?', :method => :delete %>
<% end %>
