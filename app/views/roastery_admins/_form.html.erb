<%= form_for(@roastery_admin) do |f| %>
	<%= f.hidden_field :roastery_id, :value => @roastery.id %>
	<!-- <%= f.label :user_id %> -->
	<%= f.collection_select(:user_id, User.find(:all), :id, :email, :prompt => "Select User")%>
	<%= f.submit "Approve Admin", :class => "button"%>
<% end %>
