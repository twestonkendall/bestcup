$("section[id*=<%= @cupping_flavor.id %>]").hide();

if ($("section:visible").length < 2){
	$('a').click(function() { return false; });
	window.location = 'cuppings/<%= @cupping_flavor.sample.cupping.id %>'
}